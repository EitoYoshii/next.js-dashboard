// // app/dashboard/page.tsx（サーバーコンポーネント）
// import { auth } from '@/auth'; // NextAuth({...}) から export した auth

// export default async function Page() {
//   const session = await auth();

//   return <div>こんにちは、{session?.user?.name ?? 'ゲスト'} さん</div>;
// }

